%h1 Edit Task
= form_with(model: @task, url: development_plan_task_url, local: true) do |f|
  = render 'form', f: f
  = f.submit "Update", class: "btn btn-success"
  = link_to "Back", :back, class: "btn btn-info"
