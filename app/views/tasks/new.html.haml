%h1 Create new Task
= form_with(model: @task, url: [@development_plan, @task], local: true) do |f|
  = render 'form', f: f
  = f.submit "Save", class: "btn btn-success"
  = link_to "Back", :back, class: "btn btn-info"
